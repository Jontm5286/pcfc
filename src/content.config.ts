// Placeholder — collections ahora definidas en src/live.config.ts (EmDash Live Collections).
// Este file debe estar vacío o no existir para evitar LegacyContentConfigError.
// Astro 7 native content collections NO se usan; EmDash provee el loader
// via defineLiveCollection + emdashLoader() (SSR runtime).
export {};
